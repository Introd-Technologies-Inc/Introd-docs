---
title: "Track Introduction Requests"
description: "Search, review, and maintain introduction records in the pipeline or list view."
---

Open [Introductions](https://app.getintrod.ai/introductions) to review the workspace's stored introduction requests.

## Find a record

- Search by target, company, connector, subject, message, or status text.
- Filter by a displayed stage.
- Sort by newest, oldest, or recent activity.
- Switch between **Pipeline** and **List** view.

Select a card or row to review its target, connector, relationship context, message, dates, and stored status.

## Update the workflow

In pipeline view, drag a card to the stage that best represents confirmed progress. Use **Edit** to reopen the composer context. Use **Archive** only to cancel a request and remove it from active work.

<Warning>
  Status changes and archive actions update the Introd record only. They do not send, retract, or cancel a message in email, LinkedIn, or another external channel.
</Warning>

## When the page looks wrong

| Symptom | Check |
| --- | --- |
| No introductions | Clear the search and filter, then confirm a request was saved or submitted in this workspace |
| Missing target or connector | Edit the record and confirm the source contact still exists |
| Unexpected stage | Open the record and compare its stored status with the event you confirmed externally |
| Load error | Refresh once; if it persists, contact [support](/support) with the page URL and approximate time |

For stage definitions, see [Introduction statuses](/introductions/opportunity-flow).
